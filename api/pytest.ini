[pytest]
testpaths = tests
asyncio_mode = auto
# /app is mounted read-only, so cache must live in /tmp
cache_dir = /tmp/.pytest_cache_stroom
addopts = -ra -q --strict-markers
markers =
    integration: tests that hit the live, running stroom-api container
    unit: pure-Python tests with no external dependencies
