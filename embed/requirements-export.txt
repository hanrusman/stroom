# Build-time only (export + quantisatie). torch wordt los geïnstalleerd als
# CPU-wheel (zie build-embed.sh / Dockerfile.model-export). Versies gepind voor
# reproduceerbaarheid — het int8-model is gevoelig voor de optimum/transformers/
# onnxruntime-versie waarmee het geëxporteerd is.
optimum[onnxruntime]==2.1.0
optimum-onnx==0.1.0
transformers==4.57.6
onnxruntime==1.27.0
onnx==1.22.0
